"use client";

import React, { useState, useMemo } from "react";
import { AlertTriangle, ShieldCheck, FileText, Upload, RefreshCw, CheckCircle, Lock, TrendingDown, BarChart3, Scan, Download, Save } from "lucide-react";
import Link from "next/link";

interface MatrixItem { date: string; event: string; hospitalRecord: string; expertA: string; expertB: string; riskLevel: "LOW"|"MEDIUM"|"HIGH"; strategicAlert: string; sourcePage?: string; }

const DEMO_DATA: MatrixItem[] = [
  { date: "2024-03-15 08:45", event: "Paramedics Arrive", hospitalRecord: "GCS 15/15. Alert. No head trauma.", expertA: "(Ortho p.3) 'Head strike. GCS 12 at scene.'", expertB: "(Ind Psy p.7) 'Confused — TBI, LOC.'", riskLevel: "HIGH", strategicAlert: "CRITICAL: GCS contradiction 15/15 vs 12. Subpoena original paramedic run sheet.", sourcePage: "Hosp p.1, Ortho p.3" },
  { date: "2024-03-15 09:20", event: "ED Admission", hospitalRecord: "Priority 3. Soft tissue strain. Mybulen.", expertA: "(Ortho p.4) 'Severe pain. Missed C5-C6 herniation.'", expertB: "—", riskLevel: "HIGH", strategicAlert: "ALERT: 'Soft tissue' vs C5-C6 herniation. RAF will argue degenerative.", sourcePage: "Hosp p.2, Ortho p.4" },
  { date: "2024-03-15 14:00", event: "Discharge", hospitalRecord: "Ambulatory. GP 7 days.", expertA: "(Ortho p.5) 'Unable to walk 2 weeks.'", expertB: "—", riskLevel: "MEDIUM", strategicAlert: "14-day treatment gap. Get family affidavits.", sourcePage: "Hosp p.4, Ortho p.5" },
  { date: "2024-04-01", event: "Return to Work", hospitalRecord: "(RAF1) 'Light duties. Full by Apr 15.'", expertA: "—", expertB: "(Ind Psy p.10) 'Totally incapacitated.'", riskLevel: "HIGH", strategicAlert: "FATAL: Self-reported working vs total incapacity. Interview immediately.", sourcePage: "RAF1 p.3, Ind Psy p.10" },
  { date: "2024-06-10", event: "Surgery Scheduled", hospitalRecord: "—", expertA: "(Ortho p.6) 'C5-C6 fusion. R185k.'", expertB: "(Ind Psy p.12) '18mo incapacity.'", riskLevel: "MEDIUM", strategicAlert: "Quantum: surgery + 18mo loss of earnings. Get RAF4 post-surgery.", sourcePage: "Ortho p.6, Ind Psy p.12" },
];

const NEEDS_OCR = ['application/pdf','image/png','image/jpeg','image/jpg','image/tiff','image/webp'];

export default function ChronolegalAnalysisTool() {
  const [step, setStep] = useState<1|2>(1);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string|null>(null);
  const [caseRef, setCaseRef] = useState("");
  const [attorney, setAttorney] = useState("");
  const [claimant, setClaimant] = useState("");
  const [accidentDate, setAccidentDate] = useState("");
  const [hospitalRecord, setHospitalRecord] = useState<File|null>(null);
  const [expertReportA, setExpertReportA] = useState<File|null>(null);
  const [expertReportB, setExpertReportB] = useState<File|null>(null);
  const [matrixData, setMatrixData] = useState<MatrixItem[]>([]);
  const [consented, setConsented] = useState(false);
  const [processingStage, setProcessingStage] = useState("");
  const [isDemo, setIsDemo] = useState(false);
  const [reviewedCount, setReviewedCount] = useState(0);
  const [saved, setSaved] = useState(false);
  const [saving, setSaving] = useState(false);

  const stats = useMemo(() => { const h=matrixData.filter(m=>m.riskLevel==="HIGH").length,m=matrixData.filter(m=>m.riskLevel==="MEDIUM").length,l=matrixData.filter(m=>m.riskLevel==="LOW").length; return {high:h,med:m,low:l,total:matrixData.length,quantumRisk:h*300000+m*80000}; },[matrixData]);

  const handleFileUpload = (e: React.ChangeEvent<HTMLInputElement>, slot: string) => { if(e.target.files?.[0]){const f=e.target.files[0]; if(slot==="hospital")setHospitalRecord(f); if(slot==="expertA")setExpertReportA(f); if(slot==="expertB")setExpertReportB(f);}};

  const loadDemoCase = () => { setCaseRef("RAF/2024/008921"); setClaimant("Sipho Ndlovu"); setAccidentDate("2024-03-15"); setMatrixData(DEMO_DATA); setIsDemo(true); setReviewedCount(0); setSaved(false); setStep(2); };

  const runOCR = async (file: File, label: string): Promise<string> => { setProcessingStage(`OCR: ${label}...`); const fd=new FormData(); fd.append('file',file); const res=await fetch('/api/ocr',{method:'POST',body:fd}); const data=await res.json(); if(!res.ok)throw new Error(data.error); return data.text; };

  const executeLiveAnalysis = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setError(null); if(!hospitalRecord||!expertReportA||!expertReportB){setError("All slots required.");setLoading(false);return;} try{const ht=NEEDS_OCR.includes(hospitalRecord.type)?await runOCR(hospitalRecord,'Hospital'):await hospitalRecord.text();const ea=NEEDS_OCR.includes(expertReportA.type)?await runOCR(expertReportA,'Expert A'):await expertReportA.text();const eb=NEEDS_OCR.includes(expertReportB.type)?await runOCR(expertReportB,'Expert B'):await expertReportB.text();setProcessingStage("Cross-referencing...");const fd=new FormData();fd.append("caseRef",caseRef);fd.append("claimant",claimant);fd.append("accidentDate",accidentDate);fd.append("hospitalRecord",new Blob([ht],{type:'text/plain'}),'h.txt');fd.append("expertReportA",new Blob([ea],{type:'text/plain'}),'a.txt');fd.append("expertReportB",new Blob([eb],{type:'text/plain'}),'b.txt');setProcessingStage("Building matrix...");const res=await fetch("/api/analyze",{method:"POST",body:fd});const r=await res.json();if(!res.ok)throw new Error(r.error);setMatrixData(r.data);setIsDemo(false);setReviewedCount(0);setSaved(false);setStep(2);}catch(err:any){setError(err.message);}finally{setLoading(false);setProcessingStage("");}};

  const handleSaveToDashboard = async () => {
    setSaving(true);
    try {
      await fetch('/api/cases', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ caseRef, claimant, attorney, accidentDate, matrixData, status: 'COMPLETED' }) });
      setSaved(true);
    } catch { setSaved(false); }
    finally { setSaving(false); }
  };

  const handleExportIndex = async () => {
    const payload = { metadata: { caseNumber: caseRef||"2024/29841", division: "GAUTENG DIVISION, JOHANNESBURG", plaintiff: claimant||"PLAINTIFF", defendant: "ROAD ACCIDENT FUND" }, documents: [{id:"1",sectionCode:"A",documentName:"Summons",startPage:1,endPage:12},{id:"2",sectionCode:"B",documentName:"Hospital Records",startPage:1,endPage:45},{id:"3",sectionCode:"C",documentName:"Expert Medico-Legal Reports",startPage:1,endPage:79},{id:"4",sectionCode:"D",documentName:"ChronoLegal SA Analysis Matrix",startPage:1,endPage:matrixData.length+1}] };
    const res = await fetch('/api/export-index', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify(payload) });
    if (!res.ok) { alert('Export failed.'); return; }
    const blob = await res.blob(); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = `CaseLines_Index_${(caseRef||'case').replace(/\//g,'_')}.docx`; a.click(); URL.revokeObjectURL(url);
  };

  const markReviewed = () => setReviewedCount(c=>Math.min(c+1,stats.total));
  const allFilesLoaded = hospitalRecord && expertReportA && expertReportB;
  const confPct = stats.total>0?Math.min(100,Math.round(((stats.total-stats.high+reviewedCount)/stats.total)*100)):0;

  return (
    <div className="min-h-screen bg-slate-50 font-sans text-slate-900">
      <div className="bg-slate-950 text-white text-xs py-2 px-4 flex justify-center items-center gap-6 font-mono"><span className="flex items-center gap-1.5"><Lock size={12} className="text-emerald-400"/> SQL.js Local DB · No External Services</span><span className="flex items-center gap-1.5"><ShieldCheck size={12} className="text-emerald-400"/> POPIA Compliant</span><span className="flex items-center gap-1.5"><Scan size={12} className="text-emerald-400"/> OCR + CaseLines Ready</span></div>
      <header className="bg-slate-950 text-white px-8 py-4 flex justify-between items-center shadow-md border-t border-slate-800"><Link href="/" className="flex items-center gap-3 no-underline"><div className="bg-amber-500 font-bold px-2.5 py-1 rounded text-slate-950 text-sm">CL</div><h1 className="text-xl font-bold tracking-tight">ChronoLegal <span className="text-amber-400">AI</span></h1></Link><div className="flex items-center gap-4"><button onClick={loadDemoCase} className="text-xs text-slate-400 hover:text-amber-400 font-bold">Load Demo</button><Link href="/dashboard" className="text-xs text-slate-400 hover:text-white">Dashboard</Link></div></header>

      <main className="max-w-7xl mx-auto p-8">
        <div className="flex items-center justify-center gap-12 mb-10 text-sm font-semibold text-slate-400"><div className={`flex items-center gap-2 pb-2 border-b-2 ${step===1?"text-slate-900 border-amber-500":"text-amber-600 border-transparent"}`}><span>1. Capture Bundle</span></div><div className={`flex items-center gap-2 pb-2 border-b-2 ${step===2?"text-slate-900 border-amber-500":"border-transparent"}`}><span>2. Review Matrix</span></div></div>
        {error && (<div className="bg-red-50 border border-red-200 text-red-700 p-4 rounded-xl mb-6 flex items-center gap-3 text-sm"><AlertTriangle className="h-5 w-5 shrink-0"/><p>{error}</p></div>)}

        {step === 1 && (<form onSubmit={executeLiveAnalysis} className="space-y-8 bg-white p-8 rounded-2xl border border-slate-200 shadow-sm"><div className="flex items-start justify-between"><div><h2 className="text-lg font-bold text-slate-800 border-b pb-3">Discrepancy Analysis Input</h2></div><button type="button" onClick={loadDemoCase} className="text-xs text-amber-600 hover:text-amber-800 font-bold border border-amber-300 rounded-lg px-3 py-1.5 hover:bg-amber-50">← Try Demo First</button></div><div className="grid grid-cols-1 md:grid-cols-2 gap-6"><div><label className="block text-xs font-bold uppercase tracking-wider text-slate-500 mb-2">Case Ref / RAF Number *</label><input required type="text" value={caseRef} onChange={e=>setCaseRef(e.target.value)} placeholder="RAF/2024/008921" className="w-full px-4 py-2.5 border rounded-xl focus:outline-amber-500 text-sm bg-slate-50"/></div><div><label className="block text-xs font-bold uppercase tracking-wider text-slate-500 mb-2">Claimant *</label><input required type="text" value={claimant} onChange={e=>setClaimant(e.target.value)} placeholder="Sipho Ndlovu" className="w-full px-4 py-2.5 border rounded-xl focus:outline-amber-500 text-sm bg-slate-50"/></div><div><label className="block text-xs font-bold uppercase tracking-wider text-slate-500 mb-2">Instructing Attorney</label><input type="text" value={attorney} onChange={e=>setAttorney(e.target.value)} placeholder="Your Name" className="w-full px-4 py-2.5 border rounded-xl focus:outline-amber-500 text-sm bg-slate-50"/></div><div><label className="block text-xs font-bold uppercase tracking-wider text-slate-500 mb-2">Date of Accident *</label><input required type="date" value={accidentDate} onChange={e=>setAccidentDate(e.target.value)} className="w-full px-4 py-2.5 border rounded-xl focus:outline-amber-500 text-sm bg-slate-50"/></div></div><div className="bg-blue-50 border border-blue-200 rounded-xl p-3 text-xs text-blue-800 flex items-start gap-2"><Scan size={14} className="shrink-0 mt-0.5"/><div><span className="font-bold">Pillars 1-3 Active.</span> OCR extracts text from PDFs/scans. Analysis finds contradictions. Results save to persistent dashboard.</div></div><div className="grid grid-cols-1 md:grid-cols-3 gap-6 pt-4">{[{key:"hospital",title:"Hospital Record",file:hospitalRecord,subtitle:"Admission sheets, trauma charts"},{key:"expertA",title:"Expert Report A",file:expertReportA,subtitle:"Orthopaedic surgeon, neurosurgeon"},{key:"expertB",title:"Expert Report B",file:expertReportB,subtitle:"Occupational therapist, ind. psychologist"}].map(slot=>(<div key={slot.key} className={`border-2 border-dashed rounded-2xl p-6 text-center flex flex-col items-center justify-center transition-all ${slot.file?"border-emerald-500 bg-emerald-50/30":"border-slate-200 hover:border-amber-400 bg-slate-50/50"}`}>{slot.file?<FileText className="h-10 w-10 text-emerald-600 mb-3"/>:<Upload className="h-10 w-10 text-slate-400 mb-3"/>}<h3 className="text-sm font-bold text-slate-700">{slot.title}</h3><p className="text-xs text-slate-400 px-2 mt-1 mb-4">{slot.subtitle}</p><label className={`cursor-pointer px-4 py-2 rounded-xl text-xs font-bold shadow-sm transition-all ${slot.file?"bg-emerald-600 text-white":"bg-white border text-slate-700 hover:bg-slate-100"}`}>{slot.file?"Loaded ✓":"Choose File"}<input type="file" accept=".txt,.pdf,.png,.jpg,.jpeg,.tiff,.webp" onChange={e=>handleFileUpload(e,slot.key)} className="hidden"/></label>{slot.file&&<span className="text-[10px] text-slate-500 mt-2 truncate">{slot.file.name}{NEEDS_OCR.includes(slot.file.type)?' (OCR)':''}</span>}</div>))}</div><div className="bg-slate-50 border border-slate-200 rounded-xl p-4 text-xs text-slate-600"><div className="flex items-start gap-2 mb-3"><ShieldCheck className="h-4 w-4 text-slate-500 mt-0.5 shrink-0"/><div><span className="font-bold block mb-1">POPIA Notice</span><p>Files processed locally, encrypted, deleted within 24hrs. Only anonymized matrix retained in SQL.js database.</p></div></div><label className="flex items-center gap-2 cursor-pointer"><input type="checkbox" checked={consented} onChange={e=>setConsented(e.target.checked)} className="rounded border-slate-300 text-amber-500"/><span className="font-medium">I confirm consent and accept terms.</span></label></div><div className="pt-6 border-t flex justify-between items-center"><p className="text-xs text-slate-400">PDFs, images, text · OCR · Save to dashboard</p><button type="submit" disabled={loading||!allFilesLoaded||!consented} className="bg-slate-950 text-white font-bold px-8 py-3 rounded-xl flex items-center gap-3 shadow hover:bg-slate-900 transition-all disabled:opacity-40">{loading?<><RefreshCw className="h-5 w-5 animate-spin text-amber-400"/>Analyzing...</>:"Execute Discrepancy Engine"}</button></div>{loading&&processingStage&&(<div className="text-center py-4"><p className="text-sm text-slate-500 font-mono animate-pulse">{processingStage}</p></div>)}</form>)}

        {step === 2 && (<div className="space-y-6">
          <div className="bg-white border border-slate-200 rounded-2xl shadow-sm p-6">
            <div className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4 mb-4"><div><span className="text-xs font-mono px-2.5 py-1 bg-amber-100 text-amber-800 rounded-full font-bold">{isDemo?"DEMO":"LIVE MATRIX"}</span><h2 className="text-xl font-extrabold text-slate-800 mt-2">{claimant||"Claimant"}</h2><p className="text-xs text-slate-400 mt-1 font-mono">Ref: {caseRef} · {accidentDate}{isDemo?" · Sample":""}</p></div><div className="flex flex-wrap items-center gap-2"><button onClick={handleSaveToDashboard} disabled={saved||saving||isDemo} className={`border font-bold text-xs px-4 py-2.5 rounded-xl flex items-center gap-2 transition-all ${saved?'bg-emerald-500 text-white border-emerald-500':saving?'bg-slate-100 text-slate-400':'bg-white text-slate-700 hover:bg-slate-50 border-slate-200'}`}><Save size={14}/>{saved?'Saved to Dashboard':saving?'Saving...':'Save to Dashboard'}</button><button onClick={handleExportIndex} className="border font-bold text-xs px-4 py-2.5 rounded-xl bg-slate-950 text-white hover:bg-slate-900 flex items-center gap-2"><Download size={14}/>CaseLines .docx</button><button onClick={()=>setStep(1)} className="border font-bold text-xs px-4 py-2.5 rounded-xl bg-white text-slate-700 hover:bg-slate-50">← New</button></div></div>
            <div className="grid grid-cols-2 md:grid-cols-5 gap-4 mt-4 pt-4 border-t border-slate-100"><div className="bg-red-50 border border-red-100 rounded-xl p-3 text-center"><div className="text-2xl font-extrabold text-red-700">{stats.high}</div><div className="text-[10px] font-bold text-red-600 uppercase mt-1">Critical</div></div><div className="bg-amber-50 border border-amber-100 rounded-xl p-3 text-center"><div className="text-2xl font-extrabold text-amber-700">{stats.med}</div><div className="text-[10px] font-bold text-amber-600 uppercase mt-1">Significant</div></div><div className="bg-blue-50 border border-blue-100 rounded-xl p-3 text-center"><div className="text-2xl font-extrabold text-blue-700">{stats.low}</div><div className="text-[10px] font-bold text-blue-600 uppercase mt-1">Minor</div></div><div className="bg-slate-50 border border-slate-200 rounded-xl p-3 text-center"><div className="text-2xl font-extrabold text-slate-700">{confPct}%</div><div className="text-[10px] font-bold text-slate-500 uppercase mt-1">Confidence</div></div><div className="bg-amber-50 border border-amber-200 rounded-xl p-3 text-center"><TrendingDown size={14} className="text-amber-600 inline mr-1"/><span className="text-lg font-extrabold text-amber-700">R{stats.quantumRisk.toLocaleString()}</span><div className="text-[10px] font-bold text-amber-600 uppercase mt-1">Quantum at Risk</div></div></div>
            {stats.high>0&&(<div className="mt-4 p-3 bg-red-50 border border-red-200 rounded-lg text-xs text-red-800 flex items-start gap-2"><AlertTriangle className="h-4 w-4 shrink-0 mt-0.5"/><div><span className="font-bold">{stats.high} critical contradictions.</span> If unaddressed, RAF's team will exploit — reducing quantum by ~R{stats.quantumRisk.toLocaleString()}.</div></div>)}
          </div>
          <div className="bg-white border border-slate-200 rounded-2xl overflow-hidden shadow-sm"><div className="overflow-x-auto"><table className="w-full border-collapse text-left text-sm"><thead className="bg-slate-950 text-white font-mono uppercase text-xs tracking-wider"><tr><th className="p-4 border-b w-28">Date</th><th className="p-4 border-b w-40">Event</th><th className="p-4 border-b">Hospital</th><th className="p-4 border-b">Expert A</th><th className="p-4 border-b">Expert B</th><th className="p-4 border-b w-24">Risk</th></tr></thead><tbody className="divide-y divide-slate-200">{matrixData.map((item,index)=>{const isHigh=item.riskLevel==="HIGH";const isMed=item.riskLevel==="MEDIUM";return(<React.Fragment key={index}><tr className={`transition-all ${isHigh?"bg-red-50/40":isMed?"bg-amber-50/30":"hover:bg-slate-50"}`}><td className="p-4 font-mono font-bold text-slate-600 align-top text-xs">{item.date}</td><td className="p-4 font-bold text-slate-800 align-top">{item.event}</td><td className="p-4 text-slate-600 text-xs align-top">{item.hospitalRecord||"—"}</td><td className="p-4 text-slate-600 text-xs align-top">{item.expertA||"—"}</td><td className="p-4 text-slate-600 text-xs align-top">{item.expertB||"—"}</td><td className="p-4 align-top"><span className={`inline-flex items-center gap-1.5 px-2.5 py-1 text-xs font-bold rounded-full ${isHigh?"bg-red-100 text-red-800":isMed?"bg-amber-100 text-amber-800":"bg-emerald-100 text-emerald-800"}`}>{isHigh&&<AlertTriangle className="h-3 w-3"/>}{item.riskLevel}</span></td></tr>{(isHigh||isMed)&&(<tr className={isHigh?"bg-red-50/40":"bg-amber-50/30"}><td colSpan={6} className="px-4 pb-4 pt-0"><div className={`p-4 rounded-xl text-xs flex items-start gap-3 border ${isHigh?"border-red-200 bg-white text-red-900":"border-amber-200 bg-white text-amber-900"}`}><ShieldCheck className={`h-4 w-4 shrink-0 mt-0.5 ${isHigh?"text-red-600":"text-amber-600"}`}/><div className="flex-1"><span className="font-extrabold uppercase font-mono text-[10px] block mb-1">Vulnerability Warning:</span><p className="mb-2">{item.strategicAlert}</p>{item.sourcePage&&(<div className="flex items-center justify-between text-[10px] font-mono text-slate-400 border-t border-slate-200 pt-2 mt-2"><span>📄 {item.sourcePage}</span><button onClick={markReviewed} className="text-amber-600 hover:text-amber-800 font-bold">✓ Reviewed</button></div>)}</div></div></td></tr>)}</React.Fragment>);})}</tbody></table></div></div>
        </div>)}
      </main>
    </div>
  );
}
